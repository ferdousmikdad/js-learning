
// Ternary Operator or Conditional operator, 
// which is on of my favorite operators in JS

// If a customer has more than 100 points, 
// they are a 'gold' customer, otherwise,
// they are a 'silver' customer.

let points = 110;
let type = points > 100 ? 'gold' : 'silver';

console.log(type);

// You should try to change the number of points to 90 and see what you get.
// Your answer will be 'silver'