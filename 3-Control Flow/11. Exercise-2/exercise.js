
// Write a function that takes two numbers and <br />
// and returns the maximum of the two.
// Call that function, give a different argument
// and make sure it's working properly
let number = max(5, 10);
console.log(number);

function max(a, b) {
    return (a > b) ? a : b;
}